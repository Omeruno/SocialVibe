package com.socialvibe.app.ui.theme

import androidx.compose.material3.Typography

// Stock Material3 type scale — this app never customized it, and no
// generated template ever actually created this file (verified while
// auditing the whole project): Theme.kt has referenced an undefined
// `Typography` symbol since the very first commit.
val Typography = Typography()
